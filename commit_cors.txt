fix: allow LAN origins (192.168.x, 10.x, 172.16-31.x) in CORS

Cashiers on the local network access the server via its LAN IP.
Previously only a hardcoded IP list was allowed, causing 500 errors
on login from any machine not explicitly whitelisted.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
