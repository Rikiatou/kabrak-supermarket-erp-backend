Fix cloud sync: POs rejected with 500 + stub name in English

- upsertPurchaseOrder: strip syncStatus/syncedAt/updatedAt from payload,
  fix date field name (was orderDate), strip purchaseOrderId from items
- syncEntity: log HTTP errors when response not ok (was silently dropping)
- ensureStub: use 'Pending sync' constant instead of hardcoded French

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
